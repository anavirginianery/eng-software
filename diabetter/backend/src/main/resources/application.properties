
# comentado para implementação de h2
# spring.application.name=diabetter
# # PostgreSQL Database Configuration
# spring.datasource.url=jdbc:postgresql://localhost:5432/diabetter
# spring.datasource.username=postgres
# spring.datasource.password=senha123
# spring.datasource.driver-class-name=org.postgresql.Driver

# H2 Database Configuration
spring.datasource.url=jdbc:h2:mem:diabetter;DB_CLOSE_ON_EXIT=FALSE;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password

# Hibernate (optional, for JPA)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
